<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusCat"
        tools:targetApi="31">

        <!-- ✅ HomeActivity를 앱의 시작 액티비티로 설정 -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔻 필요 시 MainActivity는 그냥 실행만 가능하도록 남겨둠 -->
        <activity android:name=".PlannerMainActivity" />
        <activity android:name=".CatDetailActivity" />
        <activity android:name=".MiniGameActivity" />

    </application>

</manifest>
